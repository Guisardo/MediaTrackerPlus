# Ralph Progress Log
Started: Fri Mar 13 23:08:58 -03 2026
---

## Codebase Patterns
- Logger import: `import { logger } from 'src/logger'` (not 'src/logger/logger')
- Module path aliases: `src/` maps to `server/src/` (baseUrl is `.` in tsconfig with `src/` prefix)
- Config class uses static readonly fields for raw env values
- jest.mock() factory cannot reference out-of-scope variables (use jest.fn() directly inside factory, then import the mock and use it)
- Tests use `jest.mock('src/...')` for module mocking with babel-jest
- tmdbLang array and TmdbLang type are in `server/src/entity/configuration.ts`
- Config static fields are `static readonly` — cast to `unknown as { field: type }` in tests to mutate mock
- TV show `localizedDetails()` mirrors `details()` structure: 1 show call + N season calls with per-season episode extraction
- Season/episode ID mapping in updateMediaItem: build Maps from `updatedMediaItem.seasons` (which has DB IDs after `margeTvShow()`) keyed by `seasonNumber` and `seasonNumber:episodeNumber`
- `updatedMediaItem.seasons` type is a union — cast to `TvSeason[]` when inside `mediaType === 'tv'` check
- Season-level API failures in localizedDetails: catch per-season, set episodes to `[]`, log error, continue with remaining seasons
- `upsertSeasonTranslation` and `upsertEpisodeTranslation` follow exact same pattern as `upsertMediaItemTranslation` (async functions, not class-based)
- Translation loop in updateMediaItem: first-language data from base `details()` + localized data for each language from `localizedDetails()` — both movie-level AND season/episode level for TV
- IGDB private `get(endpoint, query)` helper handles token refresh + RequestQueue automatically — reuse it for all IGDB API calls
- Mock RequestQueue in tests as: `{ request: jest.fn((fn) => fn()) }` to execute the callback immediately
- IGDB_REGION_MAP region 8 (worldwide) uses sentinel 'all' — replace with `getMetadataLanguages()` in updateMetadata.ts loop
- Backfill translations uses `createLock()` for its own lock (separate from updateMetadata lock) — `lockedBackfill` prevents concurrent backfill runs
- `startBackfillIfNeeded()` entry point uses `setImmediate()` to avoid blocking server startup — called in `Server.listen()` after resolve()
- `mediaItemRepository.seasonsWithEpisodes(mediaItem)` returns seasons with episodes for building season/episode ID lookup maps during backfill
- Test DB isolation: when tests fail with "UNIQUE constraint failed" in `--runInBand` mode, clean up prior data in `beforeAll` before seeding (e.g., `await Database.knex('user').del()`)
---

## 2026-03-13 - US-001
- Implemented Configure METADATA_LANGUAGES and AUDIBLE_LANG_MAP environment variables
- Files changed:
  - server/src/config.ts: Added METADATA_LANGUAGES (string[] | null) and AUDIBLE_LANG_MAP (string | null) static readonly fields
  - server/src/metadataLanguages.ts: New module with getMetadataLanguages(), getAudibleLangMap(), toTmdbLang(), and cache reset helpers for tests
  - server/__tests__/metadataLanguages.test.ts: 22 unit tests covering all parsing edge cases
- **Learnings for future iterations:**
  - Patterns discovered: Config class uses `static readonly` fields reading from process.env; metadataLanguages.ts uses module-level memoization (null check on cache variable)
  - BCP 47 tags (es-419, pt-BR) are stored as-is in the languages list; only the base subtag is used for TMDB API calls via toTmdbLang()
  - jest.mock() factories must use jest.fn() inline — cannot reference variables defined outside the factory function
  - To mutate mock Config fields in tests: cast `Config as unknown as { field: type }` and assign directly
  - Cache reset helper functions (_resetMetadataLanguagesCache, _resetAudibleLangMapCache) are exported for test isolation
  - Full test suite: 104 suites, 1333 tests passing
---
## 2026-03-13 - US-002
- Implemented Create translation database schema (mediaItemTranslation, seasonTranslation, episodeTranslation)
- Files changed:
  - server/src/migrations/20260314000000_translationTables.ts: Migration creating three translation tables with FK cascades and composite unique constraints
  - server/__tests__/migrations/translationTables.test.ts: 6 integration tests covering schema structure, upsert behavior, cascade deletion, and migration down rollback
- **Learnings for future iterations:**
  - Patterns discovered: Migration files follow idempotent pattern (check hasTable before creating); use `.onDelete('cascade')` on FK references; composite unique constraints via `.unique(['col1', 'col2'])` pattern
  - JSON columns use `.jsonb()` (Knex auto-converts to TEXT for SQLite); test inserts must stringify JSON for SQLite compatibility
  - Required fields for season: title, isSpecialSeason; required fields for episode: title, isSpecialEpisode, seasonAndEpisodeNumber, seasonId
  - When deleting parent records in tests, delete in reverse dependency order (children first) to avoid FK constraint violations
  - Tables dropped in correct order in down() migration: episodeTranslation → seasonTranslation → mediaItemTranslation
  - Full test suite: 105 suites, 1339 tests passing (6 new tests added)
---
## 2026-03-13 - US-003
- Implemented Add localizedDetails to MetadataProvider + implement TMDB movie translations
- Files changed:
  - server/src/metadata/metadataProvider.ts: Added optional `localizedDetails?(ids, language)` method following existing `similar?()` pattern
  - server/src/metadata/provider/tmdb.ts: Implemented TMDbMovie.localizedDetails calling GET /3/movie/{id}?language={lang}; deletes originalTitle; converts empty strings to null
  - server/src/repository/translationRepository.ts: New module with upsertMediaItemTranslation using knex onConflict(['mediaItemId','language']).merge()
  - server/src/updateMetadata.ts: Modified updateMediaItem() to upsert first-language data from base details() and loop over getMetadataLanguages() calling localizedDetails() for each language
  - server/__tests__/__utils__/utils.ts: Added drop calls for translation tables in clearDatabase() (episodeTranslation → seasonTranslation → mediaItemTranslation before their parent tables)
  - server/__tests__/repository/translationRepository.test.ts: 14 database integration tests (insert, upsert conflict, genres JSON, nulls)
  - server/__tests__/metadata/tmdbLocalizedDetails.test.ts: 18 unit tests (API call params, localized data, no originalTitle, empty string → null conversions)
- **Learnings for future iterations:**
  - Patterns discovered: Optional methods on abstract classes use same `similar?()` pattern with `?` suffix
  - `delete movie.originalTitle` is the correct way to remove a property from the mapped result before returning
  - For upsert in Knex: `.insert(row).onConflict(['col1','col2']).merge()` — must pass composite columns as array
  - genres stored as JSON.stringify in SQLite (TEXT column) — retrieve with JSON.parse in tests
  - clearDatabase utility at server/__tests__/__utils__/utils.ts must be updated when new FK-dependent tables are added — drop child tables before parent tables
  - upsertMediaItemTranslation exists at `src/repository/translationRepository` (not a class-based repo, just exported async functions)
  - updateMediaItem() translation loop: check `metadataProvider.localizedDetails != null` before looping; log ERROR and continue on per-language failure
  - Full test suite: 107 suites, 1373 tests passing (32 new tests added)
---
## 2026-03-14 - US-004
- Implemented TMDB TV show translation pipeline (seasons and episodes)
- Files changed:
  - server/src/metadata/provider/tmdb.ts: Implemented TMDbTv.localizedDetails — fetches show + per-season data with explicit language param; deletes originalTitle; converts empty strings to null at show, season, and episode levels; catches per-season API failures
  - server/src/repository/translationRepository.ts: Added upsertSeasonTranslation and upsertEpisodeTranslation functions with same onConflict().merge() pattern
  - server/src/updateMetadata.ts: Wired TV translation loop — builds season/episode ID lookup Maps from updatedMediaItem.seasons, upserts first-language base data for seasons/episodes, and upserts localized data for each language from localizedDetails()
  - server/__tests__/migrations/translationTables.test.ts: Fixed pre-existing bug — missing `await` on `expect(...).rejects.toThrow()` for unique constraint test
  - server/__tests__/metadata/tmdbTvLocalizedDetails.test.ts: 24 unit tests (API call params, localized show/season/episode data, originalTitle exclusion, empty string→null, season API failure handling, no-seasons edge case)
  - server/__tests__/repository/seasonEpisodeTranslationRepository.test.ts: 15 database integration tests (insert, upsert conflict, null handling for season and episode translations)
  - server/__tests__/updateMediaItemTvTranslations.test.ts: 4 integration tests (full translation wiring for TV shows, first-language base data upsert, error resilience when one language fails, no season/episode translations for movies)
- **Learnings for future iterations:**
  - TMDbTv.localizedDetails structure: 1 GET /3/tv/{id}?language={lang} call + N GET /3/tv/{id}/season/{seasonNumber}?language={lang} calls — episodes are extracted from season responses
  - Season/episode ID mapping strategy: after margeTvShow() resolves DB IDs, build `Map<seasonNumber, seasonId>` and `Map<"seasonNumber:episodeNumber", episodeId>` for fast lookups during translation upserts
  - TypeScript union type issue: `updatedMediaItem.seasons` can be `TvSeason[] | TvSeasonForProvider[]` — cast to `TvSeason[]` inside `mediaType === 'tv'` guard since margeTvShow() guarantees the concrete type
  - Per-season API failure handling: catch errors per-season in localizedDetails(), set episodes to empty array, log error with context (season number, tmdbId, language), continue with remaining seasons
  - Test mock pattern for multi-call methods: chain `.mockResolvedValueOnce()` for sequential API calls (1 TV show + N season responses)
  - Full test suite: 110 suites, 1416 tests (43 new tests added)
---
## 2026-03-14 - US-005
- Implemented Audible, IGDB, and OpenLibrary best-effort translations
- Files changed:
  - server/src/metadata/provider/audible.ts: Added localizedDetails() method — uses getAudibleLangMap() to resolve language to Audible country code, fetches from matching domain; returns null when no mapping exists
  - server/src/metadata/provider/igdb.ts: Added fetchGameLocalizations() method — single POST /v4/game_localizations with `where game = {igdbId}`; returns [{regionId, name}] filtered array; logs DEBUG message about no description
  - server/src/metadata/provider/openlibrary.ts: Added DEBUG log in details() noting no language filter applied
  - server/src/metadata/metadataProvider.ts: Added optional fetchGameLocalizations?() method to MetadataProvider abstract class
  - server/src/metadataLanguages.ts: Added IGDB_REGION_MAP constant mapping region IDs 1-9 to ISO code arrays (region 8 = 'all' sentinel)
  - server/src/updateMetadata.ts: Added IGDB game localizations loop after localizedDetails loop; maps region -> ISO codes via IGDB_REGION_MAP, intersects with METADATA_LANGUAGES, upserts title-only rows
  - server/__tests__/metadata/audibleLocalizedDetails.test.ts: 16 unit tests (domain per language, BCP 47 extraction, no-mapping returns null, error handling)
  - server/__tests__/metadata/igdbFetchGameLocalizations.test.ts: 18 unit tests (API call, result mapping, filtering, empty results, IGDB_REGION_MAP coverage for all 9 regions)
  - server/__tests__/metadata/openlibraryPrimaryLanguage.test.ts: 4 unit tests (debug log emitted, correct data returned, no localizedDetails method)
- **Learnings for future iterations:**
  - Audible countryCode is a string key used in the `this.languages` map — AudibleCountryCode is imported from `src/entity/configuration`
  - IGDB `fetchGameLocalizations` uses the same `this.get(endpoint, query)` private helper as all other IGDB queries — the `get()` method handles token refresh and RequestQueue automatically
  - IGDB region IDs in `game_localizations` response: the `region` field is a numeric ID (1-9) — map via IGDB_REGION_MAP
  - For region 8 (worldwide), use `getMetadataLanguages()` as the target language list — the sentinel 'all' means apply to every configured language
  - RequestQueue mock pattern: jest.mock 'src/requestQueue' as `{ request: jest.fn((fn) => fn()) }` to execute the callback immediately in tests
  - IGDB token refresh check: the `refreshToken()` guard checks `this.tokenAcquiredAt + expires_in > now()` — in tests, mock the Twitch POST to return a future-expiring token
  - Full test suite: 113 suites, 1450 tests (34 new tests added)
---
## 2026-03-14 - US-006
- Implemented Serve localized metadata via API for media items using Accept-Language header
- Files changed:
  - server/src/localeResolver.ts: New module exporting resolveLocale() — uses `accept` npm package for RFC 9110 Accept-Language parsing, normalizes matched value to lowercase, verifies against availableLanguages list (case-insensitive) to return canonical form
  - server/src/repository/translationRepository.ts: Added getMediaItemTranslations(), getSeasonTranslations(), getEpisodeTranslations() batch fetch functions returning Map<id, translation> — used for per-request batch SELECT instead of per-row JOINs
  - server/src/entity/mediaItem.ts: Added metadataLanguage?: string | null to both MediaItemDetailsResponse and MediaItemItemsResponse types
  - server/src/knex/queries/details.ts: Added language?: string | null param; after building base response, batch-fetches mediaItemTranslation and overlays title/overview/genres; also overlays season and episode translations via batch fetches; metadataLanguage defaults to null
  - server/src/knex/queries/items.ts: Added applyTranslationOverlay() helper that batch-fetches translations for all item IDs and overlays fields; modified getItemsKnex() to accept language arg and call overlay; metadataLanguage set per item (null when no translation)
  - server/src/repository/mediaItem.ts: Added language?: string | null to GetItemsArgs type and mediaItemRepository.details() params
  - server/src/controllers/item.ts: Added resolveMetadataLanguage() helper implementing three-tier fallback (exact → first language → null); reads Accept-Language header and passes resolved language to repository
  - server/src/controllers/items.ts: Added resolveMetadataLanguage() helper and Accept-Language resolution to getPaginated, get, and getRandom methods
  - server/src/controllers/search.ts: Added resolveMetadataLanguage() helper and Accept-Language resolution to both IMDB-ID and normal search paths
  - server/__tests__/__utils__/request.ts: Added requestHeaders?: Record<string, string> to args; defaulted all req properties (params, query, body, headers) to {} to prevent destructuring errors
  - server/__tests__/localeResolver.test.ts: 13 unit tests (exact match, BCP 47, quality values, no match, undefined header, wildcard, case-insensitivity, complex headers)
  - server/__tests__/controllers/localeResolution.test.ts: 10 integration tests covering all three fallback tiers for MediaItemController.details, ItemsController.get, ItemsController.getPaginated
- **Learnings for future iterations:**
  - `accept` npm package returns header value as-is (not lowercased) — always normalize via `matched.toLowerCase()` then find the canonical form from `availableLanguages` using case-insensitive compare
  - resolveMetadataLanguage() three-tier fallback pattern: `resolveLocale()` for exact match → `availableLanguages[0]` for tier 2 → null when `availableLanguages.length === 0`
  - Translation overlay is done post-query (batch fetch then merge) rather than SQL JOIN — avoids modifying complex query builders and satisfies batch SELECT acceptance criterion
  - When using request() test helper, always pass `requestHeaders` for Accept-Language; the helper now defaults all req properties to `{}` so undefined is safe
  - Test isolation for Config: mutate `(Config as unknown as { METADATA_LANGUAGES: string[] | null }).METADATA_LANGUAGES` in beforeEach/afterEach + call `_resetMetadataLanguagesCache()` to prevent cross-test contamination
  - Pre-existing flaky test: `translationTables.test.ts` unique constraint test intermittently fails in full suite (race condition with DB isolation) — not caused by US-006 changes; verified by running full suite before changes (10 failures) vs after (1 failure, same test)
  - Full test suite: 115 suites, 1473 tests (1 failure pre-existing, 23 new tests added)
---
## 2026-03-14 - US-007
- Implemented Serve localized season and episode data via API
- Files changed:
  - server/src/entity/tvseason.ts: Added `metadataLanguage?: string | null` to TvSeason type
  - server/src/entity/tvepisode.ts: Added `metadataLanguage?: string | null` to TvEpisode type
  - server/src/knex/queries/details.ts: Updated season/episode translation overlay to set `metadataLanguage` on each season and episode (language when translation found, null otherwise); also sets `metadataLanguage: null` on episodes without translations within the same season
  - server/__tests__/controllers/seasonEpisodeLocaleResolution.test.ts: 10 integration tests covering all three fallback tiers for both seasons and episodes
- **Learnings for future iterations:**
  - The season/episode translation overlay was already implemented in details.ts as part of US-006 (batch SELECT, map-based lookup) — US-007 only needed to add the `metadataLanguage` field to entity types and set it in the overlay
  - When the resolved language is the tier-2 fallback (first configured language), the `metadataLanguage` field on each season/episode reflects that fallback language (not the requested locale)
  - Episodes without translations get `metadataLanguage: null` even if sibling episodes in the same season have translations
  - Test parallelism: when running a single test file in isolation with Jest, the Database singleton can conflict with other test suites; use `--runInBand` for isolated test runs or rely on the full suite (which handles DB isolation correctly via `clearDatabase` + `runMigrations`)
  - Full test suite: 116 suites, 1483 tests (10 new tests added)
---

## 2026-03-14 - US-008
- Implemented Frontend sends user locale on all media requests via Accept-Language header
- Files changed:
  - client/src/api/api.ts: Added i18n import; modified customFetch to inject Accept-Language header with current i18n.locale; preserves existing headers via Headers API
  - client/src/api/__tests__/api.test.ts: Added mock for @lingui/core; added 3 new test cases verifying Accept-Language header injection
- **Learnings for future iterations:**
  - Frontend API client architecture: uses custom `customFetch` wrapper around native `fetch` API; the MediaTracker API client (auto-generated from swagger) merges headers via HttpClient.mergeRequestParams()
  - All API calls go through the single `mediaTrackerApi.customFetch` function — this is the ideal injection point for global headers
  - i18n.locale is a getter property from @lingui/core — dynamically reads the current locale at request time (not cached)
  - Headers API: `new Headers(init?.headers)` safely clones existing headers; `.set()` replaces a header value
  - Client tests mock the @lingui/core module at import time; mediatracker-api is also mocked to prevent real API calls during tests
  - Header injection is transparent to all query functions — no changes needed to individual API callers
  - Full test suite: 75 suites, 1285 tests (3 new tests added)
---

## 2026-03-14 - US-009
- Implemented Display locale badge and fallback indicator on media detail page
- Files changed:
  - client/src/components/ui/badge.tsx: New shadcn/ui Badge component with CVA variants (default, secondary, destructive, outline)
  - client/src/utils/locale.ts: New utility module with getLanguageDisplayName() and getLanguageDisplayNameInLocale() using native Intl.DisplayNames API with fallback to uppercase codes
  - client/src/components/MetadataLocaleBadge.tsx: New component that renders language badge when metadata locale matches user locale, or fallback indicator when different, or nothing when metadataLanguage is null
  - client/src/pages/Details.tsx: Added useLingui() hook import and call; integrated MetadataLocaleBadge rendering after title with i18n.locale
  - rest-api/index.ts: Regenerated via npm run build:routes to include metadataLanguage field in MediaItemDetailsResponse type
  - client/src/components/__tests__/MetadataLocaleBadge.test.tsx: 18 unit tests covering exact match, fallback indicators, BCP 47 tags, base language matching (pt vs pt-BR), case-insensitivity, accessibility
  - client/src/utils/__tests__/locale.test.ts: 15 unit tests covering simple language codes, BCP 47 tags, fallback behavior, various display locales (en, fr, es, pt, ja, zh)
- **Learnings for future iterations:**
  - Badge component: follow shadcn/ui CVA pattern with forwardRef for all UI components; `variant="secondary"` provides muted styling suitable for informational badges
  - Locale display names: use native browser Intl.DisplayNames API for zero-bundle-size solution; always provide fallback to uppercase code for unsupported browsers/codes
  - Language matching: normalize locale codes to lowercase and split on `-` to extract base language code; base matching (e.g., pt-BR matches pt) is better UX than exact matching
  - MetadataLocaleBadge logic: three tiers - exact/base match → show badge, no match → show fallback text, null → render nothing
  - i18n.locale from useLingui() is a getter that reads current locale dynamically (not cached), making it safe for locale switching without page reload
  - When Details.tsx adds new render elements, Lingui auto-extracts message strings and updates all locale .po files (just line numbers change for existing strings)
  - API type generation: npm run build:routes regenerates rest-api/index.ts from server openapi.json; must run after server type changes are committed
  - Full test suite: 77 suites, 1321 tests (33 new tests added); all passing
---

## 2026-03-14 - US-010
- Implemented Background backfill translations on initial deployment and when METADATA_LANGUAGES changes
- Files changed:
  - server/src/backfillTranslations.ts: New module with detectBackfillNeeded() (initial deployment + incremental language detection), backfillItemTranslations() (per-item localized details + IGDB game localizations), runBackfill() (batch orchestrator with 50-item batches and 35s inter-batch delay), lockedBackfill (createLock wrapper), startBackfillIfNeeded() (setImmediate entry point)
  - server/src/server.ts: Added import and call to startBackfillIfNeeded() in Server.listen() after resolve() — non-blocking background execution
  - server/__tests__/backfillTranslations.test.ts: 27 comprehensive tests covering detectBackfillNeeded (empty DB, initial deployment, incremental language addition, all languages present), backfillItemTranslations (movies, TV shows with seasons/episodes, IGDB game localizations, worldwide region, error handling, BCP 47 tags), runBackfill (batch processing, empty languages, error resilience, ID ordering), startBackfillIfNeeded (non-blocking), language removal retention, initial deployment scenario, BCP 47 tag handling
  - server/__tests__/migrations/translationTables.test.ts: Fixed pre-existing flaky unique constraint test — added cleanup of leftover rows before insert to ensure test isolation in full suite runs
  - server/__tests__/repository/translationRepository.test.ts: Fixed pre-existing DB isolation issue — added cleanup of user/mediaItem rows in beforeAll to prevent UNIQUE constraint failures when running with --runInBand
- **Learnings for future iterations:**
  - `createLock()` from `src/lock.ts` wraps an async function with a simple boolean lock preventing concurrent execution — use it for any background process that shouldn't run in parallel
  - `setImmediate()` defers execution to the next event loop iteration, ensuring the server startup (resolve()) completes before the backfill starts
  - `mediaItemRepository.seasonsWithEpisodes(mediaItem)` returns seasons with nested episodes — needed for building season/episode ID lookup Maps during backfill
  - jest.mock() factory functions MUST use jest.fn() inline — importing the mock via the module after jest.mock() gives access to the mock function for `.mockReturnValue()` calls in tests
  - Test DB isolation with `--runInBand`: clean up data from prior suites in `beforeAll` before seeding (delete user/mediaItem rows) to prevent UNIQUE constraint collisions
  - `season` table has NOT NULL constraint on `numberOfEpisodes` — always include it when inserting test season data (default to 0)
  - Full test suite: 117 suites, 1510 tests (27 new backfill tests + 2 pre-existing test fixes); all passing
---
