{
  "name": "mediatracker-plus",
  "private": true,
  "scripts": {
    "postinstall": "cd server && npm install && cd ../client && npm install",
    "build": "npm run build --prefix server && npm run build --prefix client",
    "start": "npm run start --prefix server",
    "dev:start": "npm run dev:start --prefix server",
    "format": "prettier --list-different --write .",
    "lint": "npm run lint --prefix client && npm run lint --prefix server",
    "test": "npm run test --prefix server",
    "test:client": "npm run test --prefix client",
    "test:coverage": "npm run test:coverage --prefix server",
    "test:client:coverage": "npm run test:coverage --prefix client",
    "check-types": "npm run check-types --prefix server && npm run check-types --prefix client"
  },
  "devDependencies": {
    "@types/jest": "^28.1.1",
    "prettier": "^2.6.2"
  },
  "dependencies": {
    "@types/passport": "^1.0.7",
    "url-join": "^4.0.1"
  }
}
